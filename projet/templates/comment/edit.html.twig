{% extends 'base.html.twig' %}

{% block title %}{{'comment.edit'|trans}}{% endblock %}

{% block body %}
    <h1>{{'comment.edit'|trans}}</h1>

    {{ include('comment/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('comment_index') }}">{{'back'|trans}}</a>

    {{ include('comment/_delete_form.html.twig') }}
{% endblock %}
