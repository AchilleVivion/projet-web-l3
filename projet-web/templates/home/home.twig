{% import "home/home.components.twig" as home %}
{% import 'navbar/navbar.twig' as nav  %}
{{ nav.navbar(loggedin, nav_theme) }}
<div class="row justify-content-center align-items-center h-100">
    <div class="col-md-6 h-100">
        {{ home.showNextEvent(nextEvent) }}
    </div>
    <div class="col-md-6 h-100">
        {{ home.showCommuFlow(commuFlow) }}
    </div>
</div>